enum Status {
  ATIVO
  INATIVO
  BLOQUEADO
}

type Perfil {
  id: Int
  nome: String
}

type Usuario {
  id: Int
  nome: String
  email: String
  salario: Float
  vip: Boolean
  perfil: Perfil
  status: Status
}
